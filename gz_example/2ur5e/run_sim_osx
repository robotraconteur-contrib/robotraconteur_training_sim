realpath() {
    [[ $1 = /* ]] && echo "$1" || echo "$PWD/${1#./}"
}

SCRIPT=`realpath -s $0`
BASEDIR=`dirname $SCRIPT`

export GAZEBO_MODEL_PATH=$GAZEBO_MODEL_PATH:$BASEDIR/../models

cd $BASEDIR

python -m drekar_launch --config-j2=drekar-launch.yaml.j2







